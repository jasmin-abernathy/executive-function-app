# Android validation — 15 September 2026

## Integrated scope

Preparation `d451008c1db147d76505cc4aa494698085a4e958` was reconciled with main `99e942f7eb806a70947f1c67becb47eb1f121665` without conflicts. The recent web changes remain intact. This lot adds explicit stopwatch/countdown choice, the optional local-algorithm introduction, quieter functional controls and PiP note/pause/resume actions.

Integration fixes connect the previously unused PiP action renderer and quick-note dialog, refresh actions when focus changes, and return to PiP after a successful note save or cancellation. The native PiP action never opens overlay permission settings. The separate floating service retains its existing permission flow.

The die result now scrolls into view even when its lazy item starts off-screen. Its existing optional shuffle and proposal-only widget behavior are preserved. The timer choice is scrollable; selectors retain 48 dp targets. Intro buttons use the existing pale green palette and respect system insets. An invalid Compose import in the prepared utility icons was removed.

No SQLite migration, backup-format change, permission, companion or third-party graphic dependency was added. Planning helpers already on the preparation branch remain preparation; this does not implement a full calendar or task-template UI.

## Executed checks

- Gradle 9.5.0, JDK 17.0.16, Android SDK 37: `:app:testDebugUnitTest :app:lintDebug` completed successfully.
- 53 unit/Robolectric/Compose tests: 0 failures, 0 errors, 0 skipped.
- Persistence tests cover both timer modes, interruption/resume, database reopen, attempted target replacement on resume and continued running after the target is exceeded.
- PiP action tests cover pause/resume labels, session-scoped intents and the distinct note action.
- A calm-mode widget test uses 40 tasks and starts the proposed task without manually scrolling to the result; no order mutation occurs.
- Android lint: 0 errors, 43 warnings and 2 hints. Remaining reports include existing PiP/backup configuration notices, absent application icon, resource/pluralization checks and Kotlin maintenance suggestions. No lint baseline or suppression was added.
- Kotlin grammar: all 48 source/test files passed.
- SQLite/local-data checks, FR/EN resource parity and XML parsing passed.
- Repository hygiene and staged whitespace checks passed.
- Gitleaks 8.24.2 scan of the full staged diff: no leaks found.
- Manifest and web tree unchanged from main. No untracked delivery files, APK or AAB generated. No remote workflow triggered for validation.

## Remaining device checks

These results do not replace testing on an Android device. As required by ADR 0009, verify direct PiP entry, pause/resume action refresh, note save/cancel and return to PiP on a compatible phone before merging. Also check large text, TalkBack and the visual D10 motion. No device build was authorized or produced in this pass.
